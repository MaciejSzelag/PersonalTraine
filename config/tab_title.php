

<?php
include "../app/functions/functions.php";
// $title = isset($title) ? tab_title($title) : tab_title(''); // Ustawienie domyślnego tytułu, jeśli $title nie jest ustawione
$title = getTitleFromSource($source);
?>